#include <stdio.h>

int main() {
    int a, b;
    
    // Read two integers from the user
    scanf("%d", &a);
    scanf("%d", &b);
    
    // Print the product of the two integers
    printf("%d\n", a + b);
    
    return 0;
}